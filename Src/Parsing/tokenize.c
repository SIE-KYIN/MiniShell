#include "../../Inc/minishell.h"

t_tree *tokenize(char *line)
{
    t_tree *token_tree;
    char **tmp_token;
    char *delimiter_loc;

    
    token_tree = create_tree();
    

    
    return (token_tree);
}
